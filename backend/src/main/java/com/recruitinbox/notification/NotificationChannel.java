package com.recruitinbox.notification;

/** notification_rules.channel / notifications.channel varchar(16). MVP dispatches EMAIL + IN_APP. */
public enum NotificationChannel { EMAIL, WEB_PUSH, FCM, APNS, IN_APP }
